import 'package:flutter/material.dart';

class BodyBlank extends StatelessWidget {
  final Widget child;

  BodyBlank({required this.child});

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
